.col-sm-4.col-md-3.proto-content
  .dropdown.drop_menu_location.action-Menus
    = button_tag "ACTION", class: "dropdown-btns", value: prototype.id
  .thumbnail
    = link_to image_tag(prototype.set_main_thumbnail), prototype_path(prototype)
    .caption
      %h3
        = prototype.title
      .proto-meta
        .proto-user
          = link_to prototype.user.name, user_path(prototype.user)
        .proto-posted
          = prototype.posted_date
      - if prototype.tags.present?
        %ul.proto-tag-list.list-inline
          - prototype.tags.each do |tag|
            %li
              = link_to root_path, class: "btn btn-default" do
                %span
                  = tag.name

