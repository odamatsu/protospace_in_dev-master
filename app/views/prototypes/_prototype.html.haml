.col-sm-4.col-md-3.proto-content
  .dropdown.drop_menu_location#actionMenu
    = button_tag "ACTION", id: "dropdownMenu", value: prototype.id
    -# %ul.dorpdown-menu
    -#   %li
    -#     = link_to "prototypes/#{prototype.id}", method: :delete do
    -#       Delete
    -#   %li
    -#     = link_to root_path do
    -#       Edit
  .thumbnail
    = link_to image_tag(prototype.set_main_thumbnail), prototype_path(prototype)
    .caption
      %h3
        = prototype.title
      .proto-meta
        .proto-user
          = link_to prototype.user.name, user_path(prototype.user)
        .proto-posted
          = prototype.posted_date
